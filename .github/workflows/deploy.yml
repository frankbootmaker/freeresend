name: Deploy to Kubernetes

on:
  push:
    branches: [ main ]
  workflow_dispatch:

env:
  REGISTRY: registry.digitalocean.com/curatedletters
  IMAGE_NAME: relayhorizon

jobs:
  deploy:
    name: Deploy to Digital Ocean Kubernetes
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Install doctl
      uses: digitalocean/action-doctl@v2
      with:
        token: ${{ secrets.DO_API_TOKEN }}

    - name: Log in to Digital Ocean Container Registry
      run: doctl registry login --expiry-seconds 1200

    - name: Build and push Docker image
      uses: docker/build-push-action@v5
      with:
        context: .
        platforms: linux/amd64
        push: true
        tags: |
          ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
          ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}
        cache-from: type=gha
        cache-to: type=gha,mode=max

    - name: Set up kubectl
      run: |
        doctl kubernetes cluster kubeconfig save --expiry-seconds 600 ${{ secrets.K8S_CLUSTER_NAME }}

    - name: Create registry secret
      run: |
        kubectl create secret docker-registry curatedletters \
          --docker-server=registry.digitalocean.com \
          --docker-username=${{ secrets.DO_API_TOKEN }} \
          --docker-password=${{ secrets.DO_API_TOKEN }} \
          --docker-email=${{ secrets.ADMIN_EMAIL }} \
          -n relayhorizon \
          --dry-run=client -o yaml | kubectl apply -f -

    - name: Deploy to Kubernetes
      run: |
        # Apply namespace first
        kubectl apply -f k8s/namespace.yaml

        # Note: Secrets are managed manually via kubectl apply -f k8s/secret.yaml
        # This avoids overwriting secrets on every deploy

        # Apply application manifests
        kubectl apply -f k8s/deployment.yaml
        kubectl apply -f k8s/service.yaml
        kubectl apply -f k8s/ingress.yaml
        kubectl apply -f k8s/hpa.yaml
        
        # Update image to latest version
        kubectl set image deployment/relayhorizon \
          relayhorizon=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }} \
          -n relayhorizon

    - name: Wait for deployment
      run: |
        kubectl rollout status deployment/relayhorizon -n relayhorizon --timeout=600s

    - name: Get deployment status
      run: |
        kubectl get pods -n relayhorizon
        kubectl get services -n relayhorizon  
        kubectl get ingress -n relayhorizon
        echo ""
        echo "🎉 Deployment completed!"
        echo "🌐 Application available at your configured hostname"